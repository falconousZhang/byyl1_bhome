// 规则 5.2 — for 循环（依赖 1.1, 2.0, 3.1，扩展 5.0）
// <for语句>      -> for <变量声明> in <可迭代结构> <语句块>
// <可迭代结构>   -> <表达式> '..' <表达式>
// 演示：for 范围迭代（来自 PDF 示例 program_5_20）

fn program_5_20(mut n:i32) {
    for mut i in 1..n+1 {
        n = n-1;
    }
}
#
