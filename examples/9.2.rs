// 规则 9.2 — 元组表达式（依赖 3.1，扩展 0.3）
// <可取元素>      -> '(' <元组赋值内部> ')'
// <元组赋值内部>  -> 空 | <表达式> ',' <元组元素列表>
// 演示：空元组、二元组赋值（来自 PDF 示例 program_9_2）

fn program_9_20(mut a:(i32,i32)) {
    a = (1,2);
}

fn program_9_21() {
    let mut a:(i32,i32,i32) = (1,2,3);
    let mut b:() = ();
}
#
