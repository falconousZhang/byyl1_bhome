// 规则 7.2 — 函数体为表达式块（依赖 1.1, 7.0，扩展 1.1）
// <函数声明> -> <函数头声明> <函数表达式语句块>
// 演示：函数体末尾是裸表达式，该值作为函数返回值（来自 PDF 示例 program_7_2）

fn program_7_2(mut x:i32, mut y:i32) -> i32 {
    let mut t = x*x+x;
    t = t + x*y;
    t
}
#
