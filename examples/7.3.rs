// 规则 7.3 — 选择表达式（依赖 3.1, 7.0，扩展 0.3）
// <可取元素>   -> <选择表达式>
// <选择表达式> -> if <表达式> <函数表达式语句块> else <函数表达式语句块>
// 演示：if-else 作为表达式（两分支必须均存在）（来自 PDF 示例 program_7_3）

fn program_7_3(mut a:i32) {
    let mut b = if a>0 {
        1
    } else {
        0
    };
    b;
}
#
