// 规则 8.3 — 数组元素访问（依赖 0.3, 3.1，扩展 0.3）
// <可取元素> -> <可取元素> '[' <表达式> ']'
// 演示：下标读写、连续多维下标（来自 PDF 示例 program_8_3）

fn program_8_30(mut a:[i32;3]) {
    let mut b:i32 = a[0];
    a[0] = 1;
}

fn program_8_31(mut a:[[i32;3];2]) {
    let mut b:i32 = a[0][1];
    a[1][2] = 5;
}
#
