// 规则 7.0 — 函数表达式块（依赖 1.1, 3.1，扩展 3.1）
// <函数表达式语句块>  -> '{' <函数表达式语句串> '}'
// <函数表达式语句串>  -> <表达式> | <语句> <函数表达式语句串>
// 演示：末尾为裸表达式（无分号）的块，块本身有值

fn program_7_0(mut a:i32) -> i32 {
    let mut b:i32 = {
        let mut t:i32 = a * 2;
        t + 1
    };
    return b;
}
#
