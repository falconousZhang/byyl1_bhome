// 规则 9.3 — 元组元素访问（依赖 0.3，扩展 0.3）
// <可取元素> -> <可取元素> '.' <NUM>
// 演示：通过 .0 .1 访问元组字段（来自 PDF 示例 program_9_3）

fn program_9_30(mut a:(i32,i32)) {
    let mut b:i32 = a.0;
    a.0 = 1;
}

fn program_9_31(mut a:(i32,i32,i32)) {
    a.0 = 10;
    a.1 = 20;
    a.2 = a.0 + a.1;
}
#
