// 规则 6.2 — 不可变引用（依赖 0.2, 0.3，扩展 0.2, 0.3）
// <类型>      -> '&' <类型>
// <可取引用>  -> '&' <可取引用>
// 演示：不可变引用类型声明与取引用操作（来自 PDF 示例 program_6_20）

fn program_6_20(a:i32) {
    let b:&i32 = &a;
}

fn program_6_21(a:i32) {
    let b:&i32 = &a;
    let c:& &i32 = &b;
}
#
